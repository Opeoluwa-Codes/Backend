let truth = "dick";

const fuck = (truth) => {
    return (
        "I have a big " + truth
    );
}

console.log(fuck(truth));

let lie = "head"

const oral = (lie) => {
    return (
        "I have a big " + lie
    );
}

console.log(oral(lie));


/* note to self - JUST USE CONST */
